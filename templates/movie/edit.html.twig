{% extends 'base.html.twig' %}

{% block title %}Edit Movie{% endblock %}

{% block body %}
    <div class="d-flex container-fluid justify-content-center align-items-center">
        <div class="card" style="width: 80%;">
            <h5 class="card-header">Modifier le film</h5>
            <div class="card-body">
                {{ include('movie/_form.html.twig', {'button_label': 'Modifier'}) }}
            </div>
            <div class="card-footer">
                <div class="d-flex gap-2">
                    <a class="btn btn-primary" href="{{ path('app_movie_index') }}">Retour</a>
                    {{ include('movie/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
